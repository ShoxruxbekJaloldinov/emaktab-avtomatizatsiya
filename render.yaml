services:
  - type: web
    name: emaktab-avtomatizatsiya
    env: docker
    plan: free
    dockerCommand: node server.js
    envVars:
      - key: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD
        value: "true"
      - key: PUPPETEER_EXECUTABLE_PATH
        value: "/usr/bin/google-chrome-stable"
